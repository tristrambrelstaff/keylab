<p style="color: green"><%= notice %></p>

<%= render @document %>

<div>
  <%= link_to "Edit this document", edit_document_path(@document) %> |
  <%= link_to "Back to documents", documents_path %>

  <%= button_to "Destroy this document", document_path(@document), method: :delete, form: {data: {turbo_confirm: "Are you sure?"}} %>
</div>
